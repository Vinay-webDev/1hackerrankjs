//Chocolate Feast
const {n1, c1, m1} = {n1: 15, c1: 3, m1: 2};
const {n2, c2, m2} = {n2: 10, c2: 2, m2: 5};
const {n3, c3, m3} = {n3: 12, c3: 4, m3: 4};
const chocolateFeast = (n, c, m) => {
    let choco = Math.floor(n / c);
    let chocoR = n % c;
    let r = Math.floor(choco / m) + (choco % m);
    while (r < m) {
        r = Math.ceil()
    }
}